<%= simple_form_for [@cocktail, @dose] do |f|%>
<%= f.input :description, :label => 'Dose Name' %>
<%= f.input :ingredient_id, collection: Ingredient.all %>

<%= f.submit :class => "btn btn-info"%>
<% end %>
<%= link_to "Back", :back, :class => "btn btn-info" %>

